file(GLOB_RECURSE SOURCES *.cpp)
file(GLOB_RECURSE HEADERS *.h)
string(REGEX REPLACE "\/[a-zA-Z0-9_]*\.h" "" HEADER_DIRECTORIES "${HEADERS}")
idf_component_register(SRCS ${SOURCES} INCLUDE_DIRS ${HEADER_DIRECTORIES})